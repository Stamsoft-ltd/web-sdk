# Theme Park source asset archive

Legacy template art, superseded source files, and raw conversion inputs live here.
Vite does not copy this directory into the production build.

Runtime-ready files belong in `static/`. Delete this archive after the final art
handoff if repository clone size also needs to be minimized.
