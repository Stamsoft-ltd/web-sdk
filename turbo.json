{
	"$schema": "https://turbo.build/schema.json",
	"globalDependencies": ["**/.env.*local", "**/.env"],
	"globalEnv": ["NODE_ENV", "PUBLIC_CHROMATIC", "PUBLIC_SITE_MODE", "PUBLIC_SENTRY_DSN"],
	"tasks": {
		"dev": {
			"cache": false,
			"persistent": true,
			"dependsOn": ["^build"],
			"outputs": ["dist/**"]
		},
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**", ".svelte-kit/**", ".vercel/**"]
		},
		"preview": {
			"outputs": []
		},
		"storybook": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**"]
		},
		"real-build-storybook": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**"]
		},
		"lint": {
			"outputs": []
		},
		// cache:false — forest-gang's suite reads OUTSIDE its own package (packages/pixi-svelte/src and
		// every app's static/ tree). Turbo hashes only the package, so a cached pass could replay over
		// an AnimatedSprite.svelte regression the suite exists to catch.
		"test": {
			"outputs": [],
			"cache": false
		},
		"format": {
			"outputs": []
		},
		"e2e": {
			"outputs": []
		},
		"e2e-smoke": {
			"outputs": []
		},
		"e2e-mobile": {
			"outputs": []
		}
	}
}
